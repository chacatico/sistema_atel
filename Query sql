Query
______________________________
Extrae datos: cliente, factura y vendedor relacionados entre sí.

select p.nombre, c.id_cliente, c.nombre, c.apellido, c.cedula, c.telefono, c.correo, c.direccion, f.fecha from personas as p, clientes as c, facturas as f where p.id_persona = f.idVendedor  and c.id_cliente = f.id_cliente and f.id_factura = 10
______________________________
Extrae datos por id de factura

select p.nombre_producto, p.descripcion, p.pvp from productos as p, detalle_factura as d, factura as f where d.id_factura = f.id_factura and d.id_producto = p.id_producto

select 
			d.id_factura,
			d.cantidad_producto,
			p.nombre_producto, 
			p.descripcion, 
			p.pvp 
			from 
			productos as p, 
			detalle_factura as d, 
			facturas as f 
			where 
			f.id_factura = ".$id." and 
			d.id_producto = p.id_producto
________________________________

